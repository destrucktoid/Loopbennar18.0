vir_enrichment = {
	diplomatic_reputation = 2
	sprite = 34

	potential = {
		religion = infinite_self
	}

	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_enrichment_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_enrichment }
			custom_tooltip = vir_enrichment_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_diligence = {
	discipline = 0.05

	sprite = 35

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_diligence_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_diligence }
			custom_tooltip = vir_diligence_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_acceptance = {
	war_exhaustion = -0.03

	sprite = 36

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_acceptance_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_acceptance }
			custom_tooltip = vir_acceptance_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_cooperation = {
	trade_efficiency = 0.15

	sprite = 37
	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_cooperation_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_cooperation }
			custom_tooltip = vir_cooperation_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_forbearance = {
	global_regiment_cost = -0.1

	sprite = 38

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_forbearance_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_forbearance }
			custom_tooltip = vir_forbearance_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_honesty = {
	yearly_corruption = -0.10

	sprite = 39

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_honesty_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_honesty }
			custom_tooltip = vir_honesty_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}

vir_awareness = {
	core_creation = -0.05

	sprite = 40

	potential = {
		religion = infinite_self
	}
	effect = {
		if = {
			limit = { NOT = { has_country_modifier = vir_awareness_mastered } }
			hidden_effect = { set_estate_privilege = estate_church_vir_awareness }
			custom_tooltip = vir_awareness_tt
		}
		else = {
			custom_tooltip = vir_stuck_tt
		}
	}
	ai_will_do = {
		factor = 1
	}
}